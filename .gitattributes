# Applied migrations are checksummed byte-for-byte. Keep ordinary whitespace
# checks, including trailing-space detection, but tolerate blank lines at EOF.
services/api/src/db/migrations/*.sql -text whitespace=-blank-at-eof
